import { StyleSheet} from 'react-native';

export const signupStyles = StyleSheet.create({
    container: {
        backgroundColor: '#fff',
        alignItems: 'center',
        borderRadius:10,
        elevation:2,
        marginBottom:40,
        width:344,
        marginLeft:'auto',
        marginRight:'auto',
    },
    map: {
      width:325,
      height:320,
    }
});